import type { NextConfig } from "next";

/**
 * Конфигурация Next.js.
 * Пока нам не нужно ничего настраивать — пустого объекта достаточно.
 * Типизация NextConfig подсказывает редактору все доступные опции.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
