/**
 * PostCSS — это "конвейер", через который проходит наш CSS.
 * Tailwind v4 подключается как единственный плагин.
 */
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
